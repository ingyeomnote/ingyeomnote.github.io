---
title: "[블로그 만들기 #5] Netlify와 CMS 이해하기 - 두 개의 URL로 접근하는 원리"
date: 2025-11-04T16:00:00+09:00
description: "Netlify가 무엇이고, 어떻게 GitHub 저장소와 연결되며, 왜 두 개의 URL로 같은 블로그에 접근할 수 있는지 원리를 쉽게 설명합니다."
categories: ["블로그 만들기"]
tags: ["Netlify", "CMS", "배포", "호스팅"]
draft: false
---

## 들어가며

제 블로그는 신기하게도 **두 개의 주소**로 접근할 수 있습니다:
- https://ingyeomnote.github.io
- https://ingyeomnote.netlify.app

"어? 똑같은 사이트인데 왜 주소가 두 개지?"
"Netlify가 뭐하는 건데?"
"GitHub 저장소는 어떻게 웹사이트가 되는 거야?"

이번 글에서 이 모든 궁금증을 해결합니다!

## GitHub Pages vs Netlify - 비유로 이해하기

### 실생활 비유: 음식 배달

```
[GitHub Pages]
식당(저장소) → 자체 배달(GitHub Pages) → 고객(사용자)

[Netlify]
식당(저장소) → 배달 대행(Netlify) → 고객(사용자)
```

**핵심**: 음식(웹사이트)은 같지만, 배달 방법이 다릅니다!

### 더 정확한 비유: 집 주소

```
[실제 상황]
우리 집 = 서울시 강남구 xxx

[인터넷 세상]
블로그 실제 위치 = GitHub 저장소

[주소들]
도로명 주소 = ingyeomnote.github.io
지번 주소 = ingyeomnote.netlify.app

→ 주소는 다르지만 같은 집!
```

## GitHub 저장소가 웹사이트가 되는 원리

### 1. 정적 사이트의 특징

**정적 사이트** = HTML + CSS + JS 파일들의 모음

```
웹사이트 = 폴더 구조
├── index.html          → https://example.com/
├── about/
│   └── index.html      → https://example.com/about/
└── posts/
    └── hello/
        └── index.html  → https://example.com/posts/hello/
```

**웹 서버가 하는 일**:
1. 사용자가 URL 요청
2. 해당 HTML 파일 찾기
3. 파일 내용 전송

**예시**:
```
요청: https://example.com/about/
     ↓
서버: about/index.html 파일을 찾아서
     ↓
응답: HTML 내용 전송
```

### 2. GitHub Pages의 작동 원리

```
[GitHub 저장소]
username/username.github.io
├── index.html
├── about/
└── posts/

        ↓ [GitHub Pages 서버]

[웹사이트]
https://username.github.io
├── /              → index.html
├── /about/        → about/index.html
└── /posts/        → posts/...
```

**과정**:
1. GitHub이 저장소의 파일들을 읽음
2. 웹 서버 설정
3. `username.github.io` 도메인으로 연결
4. 사용자 요청 시 파일 전송

### 3. Hugo + GitHub Pages의 흐름

```
[로컬 작업]
content/posts/hello.md
        ↓ [Hugo 빌드]
public/posts/hello/index.html
        ↓ [Git push]
[GitHub 저장소]
public/posts/hello/index.html
        ↓ [GitHub Pages]
https://username.github.io/posts/hello/
```

## Netlify란 무엇인가?

### Netlify의 정체

Netlify는 **정적 사이트 호스팅 + 배포 플랫폼**입니다.

**GitHub Pages와의 차이**:

| | GitHub Pages | Netlify |
|---|--------------|---------|
| **기본 기능** | 정적 호스팅 | 정적 호스팅 + α |
| **빌드** | GitHub Actions 필요 | 자체 빌드 기능 |
| **배포 속도** | 1~2분 | 10~30초 |
| **도메인** | username.github.io | username.netlify.app |
| **추가 기능** | 없음 | Forms, Functions 등 |
| **CMS 통합** | 수동 | Netlify Identity 제공 |

### Netlify의 강력한 기능들

#### 1. 자동 빌드
```
GitHub 저장소 연결
     ↓
Netlify가 자동으로:
- Hugo 설치
- hugo 명령어 실행
- 결과물 배포
```

GitHub Actions 없이도 자동 배포!

#### 2. 즉각적인 배포
- GitHub Pages: 1~2분
- Netlify: 10~30초

#### 3. Netlify Identity
- 사용자 인증 시스템
- CMS 로그인에 필수
- 무료 플랜: 1000명까지

#### 4. Forms
```html
<form netlify>
  <input type="email" name="email">
  <button>제출</button>
</form>
```
백엔드 없이 폼 데이터 수집!

#### 5. Deploy Previews
Pull Request마다 미리보기 사이트 자동 생성!

```
PR #123 생성
     ↓
Netlify가 임시 URL 생성:
https://deploy-preview-123--yoursite.netlify.app
```

## Netlify가 GitHub 저장소와 연결되는 과정

### 1단계: Netlify 계정 생성

1. https://netlify.com 접속
2. "Sign up with GitHub" 클릭
3. GitHub 계정으로 로그인

### 2단계: 사이트 추가

1. Netlify 대시보드에서 "Add new site" → "Import an existing project"
2. "GitHub" 선택
3. 저장소 목록에서 `username.github.io` 선택

### 3단계: 빌드 설정

```
Build command: hugo
Publish directory: public
```

Netlify가 이 명령어를 실행합니다:
```bash
hugo          # 빌드 명령어
              # ↓
              # public/ 폴더 생성
```

### 4단계: 배포!

"Deploy site" 클릭 → 자동으로 배포 시작

```
[Netlify 서버]
1. GitHub 저장소 클론
2. Hugo 설치
3. hugo 명령어 실행
4. public/ 폴더를 웹 서버로 배포
5. random-name.netlify.app 도메인 할당
```

### 5단계: 도메인 변경 (선택)

```
random-name-123.netlify.app
        ↓ [Site settings → Domain management]
ingyeomnote.netlify.app
```

## 왜 두 개의 URL로 접근할 수 있나?

### 물리적 구조

```
[GitHub 저장소]
ingyeomnote/ingyeomnote.github.io
         ↓                    ↓
[GitHub Pages 서버]    [Netlify 서버]
         ↓                    ↓
https://ingyeomnote     https://ingyeomnote
.github.io              .netlify.app
```

**핵심**: **같은 저장소**를 두 개의 서버가 각각 호스팅합니다!

### 자세한 흐름

#### 사용자 A가 GitHub Pages로 접속
```
1. 브라우저에 ingyeomnote.github.io 입력
2. DNS: "GitHub Pages 서버로 가세요!"
3. GitHub Pages 서버: 저장소 파일 전송
4. 브라우저: HTML 렌더링
```

#### 사용자 B가 Netlify로 접속
```
1. 브라우저에 ingyeomnote.netlify.app 입력
2. DNS: "Netlify 서버로 가세요!"
3. Netlify 서버: 저장소 파일 전송
4. 브라우저: HTML 렌더링
```

**결과**: 같은 파일, 다른 서버, 같은 화면!

### 실제 데이터 흐름

```
[당신이 글 작성]
git push origin master
        ↓
[GitHub 저장소 업데이트]
        ↓
   ┌────┴────┐
   ↓         ↓
[GitHub      [Netlify가
Actions       감지]
실행]         ↓
   ↓      [Netlify 빌드]
[GitHub      ↓
Pages   [Netlify 배포]
배포]
   ↓         ↓
두 사이트 모두 업데이트!
```

## Netlify의 자동 배포 원리

### Webhook 시스템

Netlify는 **Webhook**으로 저장소 변경을 감지합니다.

```
[GitHub 저장소]
git push
   ↓
"저장소가 업데이트되었어요!"
   ↓
[Webhook URL로 알림 전송]
   ↓
[Netlify 서버]
"오! 업데이트 감지!"
   ↓
[자동 빌드 시작]
```

**Webhook이란?**
- 이벤트가 발생하면 자동으로 알림을 보내는 시스템
- 비유: 집 현관 초인종 🔔

### Netlify 대시보드에서 확인

```
Site overview → Deploys

[Deploy log]
1. Fetching Git repository
2. Installing Hugo
3. Building site
4. Post processing
5. Site is live!
```

실시간으로 빌드 과정을 볼 수 있습니다!

## CMS (Content Management System)란?

### 블로그 글 작성 방법 비교

#### 방법 1: 기존 방식 (개발자 친화적)
```bash
# 1. 터미널 열기
hugo new posts/new-post.md

# 2. 에디터로 파일 열기
vim content/posts/new-post.md

# 3. 마크다운 작성
# ...

# 4. Git으로 배포
git add .
git commit -m "새 글"
git push
```

**단점**: 터미널, Git, 마크다운 지식 필요

#### 방법 2: CMS 사용 (워드프레스처럼)
```
1. https://yoursite.netlify.app/cms 접속
2. 로그인
3. "New Post" 클릭
4. 웹 에디터로 글 작성
5. "Publish" 클릭
   → 자동으로 GitHub에 커밋!
```

**장점**: 웹 브라우저만 있으면 어디서든 글 작성!

### CMS의 종류

| CMS | 방식 | 특징 |
|-----|------|------|
| **WordPress** | 동적 | 데이터베이스 필요, 무거움 |
| **Netlify CMS** | Git 기반 | GitHub에 직접 커밋 |
| **Decap CMS** | Git 기반 | Netlify CMS의 후속 |
| **Forestry** | Git 기반 | 유료, 디자인 좋음 |

### Netlify CMS (Decap CMS)의 작동 원리

```
[웹 브라우저]
/cms 페이지 접속
     ↓
[Netlify Identity로 로그인]
     ↓
[CMS 인터페이스]
글 작성 및 수정
     ↓
[GitHub API를 통해 저장소에 커밋]
     ↓
[Netlify 자동 배포]
     ↓
블로그 업데이트!
```

**핵심**: CMS는 **GitHub API를 사용해서 파일을 커밋**합니다!

```
CMS에서 "Publish" 클릭
        ↓
GitHub API: "content/posts/new-post.md 생성"
        ↓
GitHub 저장소에 커밋됨 (자동)
        ↓
Netlify 자동 배포
```

### Git Gateway란?

Netlify CMS는 **Git Gateway**를 통해 저장소에 접근합니다.

```
[CMS]
"이 글을 저장해줘!"
     ↓
[Git Gateway]
"누구세요? 권한 확인..."
     ↓
[Netlify Identity]
"이 사용자 인증됨!"
     ↓
[GitHub API]
"저장소에 커밋 완료!"
```

**왜 필요한가?**
- GitHub 토큰을 직접 노출하지 않음 (보안)
- Netlify Identity로 권한 관리
- 여러 사용자 관리 가능

## 전체 구조 총정리

### 최종 아키텍처

```
┌─────────────────────────────────────────┐
│         [GitHub 저장소]                 │
│   ingyeomnote/ingyeomnote.github.io      │
│                                         │
│   content/posts/*.md (마크다운 글)       │
│   layouts/*.html (템플릿)               │
│   static/cms/* (CMS 설정)               │
└────┬─────────────────────┬──────────────┘
     │                     │
     │                     │
 ┌───▼──────┐      ┌──────▼───────┐
 │ GitHub   │      │   Netlify    │
 │ Actions  │      │   빌드 서버   │
 │          │      │              │
 │ hugo     │      │   hugo       │
 │          │      │              │
 └───┬──────┘      └──────┬───────┘
     │                     │
 ┌───▼──────────┐  ┌──────▼─────────────┐
 │ GitHub Pages │  │  Netlify 서버      │
 │              │  │  + Identity        │
 │ .github.io   │  │  + CMS             │
 └──────────────┘  └────────────────────┘
         │                 │
         │                 │
    ┌────▼─────────────────▼────┐
    │     사용자 브라우저        │
    │                           │
    │  두 URL 모두 같은 사이트!  │
    └───────────────────────────┘
```

### 워크플로우 시나리오

#### 시나리오 1: 로컬에서 글 작성
```
1. 로컬에서 hugo new posts/article.md
2. 마크다운으로 글 작성
3. git push origin master
4. GitHub Actions 실행 → GitHub Pages 업데이트
5. Netlify Webhook 감지 → Netlify 빌드 → Netlify 배포
6. 두 URL 모두 업데이트 완료!
```

#### 시나리오 2: CMS에서 글 작성
```
1. https://ingyeomnote.netlify.app/cms 접속
2. Netlify Identity로 로그인
3. 웹 에디터로 글 작성
4. "Publish" 클릭
5. Git Gateway가 GitHub 저장소에 커밋
6. (시나리오 1의 4~6단계 동일)
```

## 왜 이렇게 복잡하게?

### Netlify를 추가하는 이유

**GitHub Pages만으로도 충분한데, 왜 Netlify?**

#### 1. CMS 관리자 기능
- Netlify Identity: 사용자 인증
- Git Gateway: 안전한 저장소 접근
- 웹에서 글 작성/수정/삭제

#### 2. 더 빠른 배포
- GitHub Pages: 1~2분
- Netlify: 10~30초

#### 3. 추가 기능
- Deploy Previews
- Form 처리
- Serverless Functions
- Analytics

#### 4. 백업 효과
- GitHub Pages 장애 시 Netlify로 접속 가능
- 두 개의 독립적인 CDN

## 다음 편 예고

다음 포스트에서는 **실제로 Netlify CMS를 설정**합니다:
- Netlify 사이트 연결
- Netlify Identity 설정
- CMS 설정 파일 작성
- 웹에서 글 작성해보기

## 마치며

처음엔 "왜 이렇게 복잡해?" 싶었지만, 이해하고 나니:
- ✅ GitHub Pages: 기본 호스팅
- ✅ Netlify: CMS 관리 + 빠른 배포
- ✅ 두 개의 URL: 같은 사이트, 다른 경로

각자의 역할이 있고, 함께 사용하면 더 강력해집니다!

다음 편에서는 실제로 설정하면서 더 명확하게 이해할 수 있을 겁니다. 🚀

---

**이전 글:** [2] Giscus 댓글 기능 추가

**다음 글:** [3-2] Netlify CMS 설정 및 사용법

**시리즈 전체 보기:**
- [1-1] Hugo와 GitHub Pages란?
- [1-2] Hugo 블로그 구조와 설정
- [1-3] GitHub Pages에 배포하기
- [2] Giscus 댓글 기능 추가
- [3-1] Netlify와 CMS 이해 ← 현재 글
- [3-2] Netlify CMS 설정
